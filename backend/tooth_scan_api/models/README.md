# Tooth Segmentation Model

Place your ONNX tooth segmentation model here:

```text
tooth_segmentation.onnx
```

The backend detector will try to load this file automatically.

You can also point to a different model path with:

```powershell
$env:TOOTH_SEGMENTATION_MODEL="D:\full\path\to\tooth_segmentation.onnx"
```

